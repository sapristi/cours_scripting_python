---
Contexte: Présentation d'une requête http. Utilisation de la librairie requests.
durée (h): 3
---
Notions clés:
- anatomie d'une URL (schéma, domaine, ressource + paramètres)
- anatomie d'une requête / réponse HTTP (URL, méthode, headers, body, status code)
- inspection au navigateur (onglet Réseau) et serveur d'écho `demo.mmill.eu`
- client HTTP `requests` en Python (`GET`, méthodes, headers custom)

---

> [!danger] Cadre légal — labo isolé uniquement
> Les requêtes des exercices visent uniquement le serveur de TP fourni (`demo.mmill.eu`) ou votre `127.0.0.1`. Pas de scan / test hors labo sans **autorisation écrite**. **Art. 323-1 Code pénal :** accès frauduleux = délit, même « juste pour voir ».

# 1. Requêtes web - Référence

![[intro.png|600]]

## 1.1 URL

Une URL (*unified resource locator*) est une chaîne de caractères particulière. Elle est constituée de plusieurs parties :

![[request_full.png|600]]
*Tiré de https://developer.mozilla.org/en-US/docs/Learn/Common_questions/Web_mechanics/What_is_a_URL — le site https://developer.mozilla.org est une très bonne référence pour tout ce qui concerne les technologies du web.*

On utilisera une version un peu plus simple :

![[request_simple.png|600]]

- *schema :* détermine le protocole utilisé pour communiquer avec le serveur distant
- *domaine :* identifiant du serveur distant (permet de trouver l'adresse IP associée)
- *ressource* + *paramètres :* chaînes de caractères quelconques (et optionnelles), interprétées par le serveur distant

## 1.2 Requête web (et sa réponse)

Une requête web est un message envoyé d'un ordinateur à un autre. L'URL est un élément essentiel d'une requête, puisqu'elle permet de savoir :
- quel est le serveur à contacter
- quel protocole utiliser

Mais ce n'est pas le seul élément qui constitue une requête.

**Éléments d'une requête :**

| Élément | Description | Exemples |
| ------- | ----------- | -------- |
| **URL** | Détermine le protocole, le serveur à contacter, et permet de passer certains paramètres | `https://some-domain/sub/path?arg1=v1&arg2=v2` |
| **Méthode** | Chaîne de caractères quelconque | `GET`, `POST`, etc. |
| **Headers** | Ensemble de paramètres « clé - valeur » | `User-Agent: Mozilla/5.0`<br>`Accept: text/html`<br>`Cookie: WMF-Last-A...` |
| **Body** | Données quelconques (optionnel) | — |

Une fois la requête envoyée, le serveur distant envoie une réponse, qui contient :

**Éléments d'une réponse :**

| Élément | Description | Exemples |
| ------- | ----------- | -------- |
| **Status code** | Permet de connaître le statut de la requête. Voir https://en.wikipedia.org/wiki/List_of_HTTP_status_codes | `200`, `400`, `500`, etc. |
| **Headers** | Ensemble de paramètres « clé - valeur » | `content-type: text/html`<br>`content-length: 25874` |
| **Body** | Réponse en elle-même — données quelconques | page html, image, donnée json, etc. |

# 2. Inspection d'une requête

## 2.1 Navigateur

Les outils développeurs du navigateur permettent d'inspecter les requêtes qui sont faites :

1. Ouvrir `wikipedia.org` dans un navigateur
2. Ouvrir les outils développeurs, onglet « Réseau », puis recharger la page
3. On peut voir les différentes requêtes web initiées par le site

![[outils_developers.png|600]]

> [!example] Exercice 1
> Retrouver les différents éléments des requêtes et réponses (cf première partie) dans l'inspecteur du navigateur.

## 2.2 Serveur exemple

Le serveur à l'adresse http://demo.mmill.eu/rec/ permet d'inspecter une requête.

Par exemple, en ouvrant le lien suivant http://demo.mmill.eu/rec/bonjour_esgi/?cours=3A , on obtient la réponse :

```text
scheme: http
method: GET
path: /rec/bonjour_esgi/

parameters:
    {
        "cours": [
            "3A"
        ]
    }
```

> [!example] Exercice 2
> Écrire l'URL permettant d'obtenir :
> ```text
> scheme: http
> method: GET
> path: /rec/un/petit/chemin
>
> parameters:
>     {
>         "localisation": [ "rennes" ],
>         "couleur": [ "verte" ]
>     }
> ```

> [!example] Exercice 3 - éditeur de requête
> En utilisant la fonction « modifier et renvoyer » du navigateur, éditer la requête pour obtenir les headers suivants :
>
> ![[modifier_renvoyer.png|600]]
>
> ```text
> parameters:
>     {
>         "cours": [ "3A" ]
>     }
>
> body:
> b'Ceci est le corps du message'
>
> headers:
>     {
>         ...
>         "Etablissement": "esgi",
>         "Cours": "3A",
>         ...
>     }
> ```

# 3. Librairie `requests`

> « Requests is an elegant and simple HTTP library for Python, built for human beings. »

- Documentation : https://requests.readthedocs.io/en/latest/
- Code source : https://github.com/psf/requests

Nous allons écrire un équivalent (très simple !) du programme `curl`, qui permet d'effectuer des requêtes web.

## 3.1 Partie 1 - simple requête

> [!info] Documentation
> Lire les sections suivantes de la page « Quickstart » :
> - https://requests.readthedocs.io/en/latest/user/quickstart/#make-a-request
> - https://requests.readthedocs.io/en/latest/user/quickstart/#passing-parameters-in-urls
> - https://requests.readthedocs.io/en/latest/user/quickstart/#response-content

> [!example] Programme de requête - partie 1
> Écrire un programme en ligne de commande qui :
>
> *Prend en argument :*
> - une URL `url`
>
> *Retourne :*
> - envoie une requête `GET` à l'URL
> - affiche le contenu de la réponse

Pour vérifier la requête envoyée, on pourra envoyer des requêtes à l'adresse `https://demo.mmill.eu/rec/<CHEMIN>`.

## 3.2 Partie 2 - méthode

> [!info] Documentation
> HTTP Verbs :
> - https://requests.readthedocs.io/en/latest/user/advanced/#http-verbs

> [!example] Programme de requête - partie 2
> 1. Ajouter au programme un argument `--method`, qui prend pour valeur un des verbes décrits dans la documentation. La valeur par défaut est `GET`.
> 2. La méthode donnée est utilisée pour effectuer la requête.

On pourra encore tester en envoyant des requêtes à l'adresse `https://demo.mmill.eu/rec/<CHEMIN>`.

## 3.3 Partie 3 - headers

> [!info] Documentation
> Requests custom headers :
> - https://requests.readthedocs.io/en/latest/user/quickstart/#custom-headers
>
> Paramètre `nargs` de argparse :
> - https://docs.python.org/3/library/argparse.html#nargs

> [!example] Programme de requête - partie 3
> 1. Ajouter au programme un argument `--header`.
> 2. Le `header` doit être une string de la forme `<KEY>:<VALUE>`.
> 3. Passer à la requête un header de la forme `{ "KEY": "VALUE" }`.
> 4. Grâce au paramètre `nargs`, permettre de passer ainsi plusieurs headers.

## 3.4 Partie 4 - Bonus

> [!example] Exercice 1 : Paramètres supplémentaires
> *Entrée :*
> Ajouter un paramètre optionnel `--mot` de type string.
>
> *Sortie* (si `--mot` est utilisé) :
> - le nombre de fois où `mot` apparaît dans le corps de la réponse

> [!example] Exercice 2 : Mesures d'une requête
> *Entrée :*
> Ajouter un paramètre optionnel `--stats` de type booléen (default `False`).
>
> *Sortie* (si `--stats` est utilisé) :
> - affiche la taille (en octets) du corps de la réponse
> - affiche le temps nécessaire pour recevoir la réponse
